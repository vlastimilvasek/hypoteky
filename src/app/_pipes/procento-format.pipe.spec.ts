import { ProcentoFormatPipe } from './procento-format.pipe';

describe('ProcentoFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcentoFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
